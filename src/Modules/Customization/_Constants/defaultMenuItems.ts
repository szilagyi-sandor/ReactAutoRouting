import { adminNavItems } from "Layouts/Admin/_Constants/adminNavItems";
import { siteNavItems } from "Layouts/Site/_Constants/siteNavItems";

export const defaultMenuItems = {
  site: siteNavItems,
  admin: adminNavItems,
};
